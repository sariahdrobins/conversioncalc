<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Advanced Blue Calculator</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .calculator {
            background-color: #1e3a8a;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 320px;
        }
        .calculator input {
            width: 100%;
            padding: 10px;
            font-size: 2em;
            text-align: right;
            border: none;
            background-color: #e0e7ff;
            color: #333;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        .calculator button {
            background-color: #3b82f6;
            border: none;
            color: white;
            font-size: 1.5em;
            padding: 20px;
            margin: 5px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
            width: 70px;
        }
        .calculator button:hover {
            background-color: #2563eb;
        }
        .calculator button:active {
            background-color: #1d4ed8;
        }
        .calculator button.operator {
            background-color: #9333ea;
        }
        .calculator button.operator:hover {
            background-color: #7c3aed;
        }
        .calculator button.double {
            width: 150px;
        }
    </style>
</head>
<body>
    <div class="calculator">
        <input type="text" id="display" disabled>
        <div>
            <button onclick="clearDisplay()">C</button>
            <button onclick="deleteLast()">←</button>
            <button onclick="appendValue('%')">%</button>
            <button onclick="appendValue('/')">/</button>
        </div>
        <div>
            <button onclick="appendValue('7')">7</button>
            <button onclick="appendValue('8')">8</button>
            <button onclick="appendValue('9')">9</button>
            <button onclick="appendValue('*')">*</button>
        </div>
        <div>
            <button onclick="appendValue('4')">4</button>
            <button onclick="appendValue('5')">5</button>
            <button onclick="appendValue('6')">6</button>
            <button onclick="appendValue('-')">-</button>
        </div>
        <div>
            <button onclick="appendValue('1')">1</button>
            <button onclick="appendValue('2')">2</button>
            <button onclick="appendValue('3')">3</button>
            <button onclick="appendValue('+')">+</button>
        </div>
        <div>
            <button onclick="appendValue('0')" class="double">0</button>
            <button onclick="appendValue('.')">.</button>
            <button onclick="calculateResult()" class="operator">=</button>
        </div>
        <div>
            <button onclick="appendValue('pi')" class="operator">π</button>
            <button onclick="appendValue('e')" class="operator">e</button>
            <button onclick="appendValue('sqrt(')" class="operator">√</button>
            <button onclick="appendValue('pow(')" class="operator">^</button>
        </div>
        <div>
            <button onclick="toggleMode()" class="operator">Deg/Rad</button>
        </div>
    </div>

    <script>
        let currentMode = 'deg';

        function clearDisplay() {
            document.getElementById('display').value = '';
        }

        function deleteLast() {
            const display = document.getElementById('display');
            display.value = display.value.slice(0, -1);
        }

        function appendValue(value) {
            document.getElementById('display').value += value;
        }

        function calculateResult() {
            let expression = document.getElementById('display').value;

            // Replace mathematical constants with their respective values
            expression = expression.replace(/pi/g, Math.PI);
            expression = expression.replace(/e/g, Math.E);

            // Handle square root and power operations
            expression = expression.replace(/sqrt\(/g, 'Math.sqrt(');
            expression = expression.replace(/pow\(/g, 'Math.pow(');

            try {
                let result = eval(expression);
                
                // Handle the degree/radian toggle
                if (currentMode === 'deg') {
                    result = result * (180 / Math.PI); // Convert radian to degree
                }

                document.getElementById('display').value = result;
            } catch (e) {
                document.getElementById('display').value = 'Error';
            }
        }

        function toggleMode() {
            if (currentMode === 'deg') {
                currentMode = 'rad';
            } else {
                currentMode = 'deg';
            }
        }
    </script>
</body>
</html>
